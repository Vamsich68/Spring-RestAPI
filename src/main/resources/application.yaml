spring:
  h2:
    console:
      enabled:true
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    defer-datasource-initialization : true
    show-sql: true
logging:
  level:
    org:
      springframework: info


#management:
  #endpoints:
    #web:
      #exposure:
        #include:*

